# ============================================
# Travel Buddy Frontend - Environment Variables
# ============================================

# Backend API Configuration
# Base URL for the Travel Buddy backend API
# Default: http://localhost:5000/api/v1
NEXT_PUBLIC_BASE_API_URL=http://localhost:5000/api/v1

# JWT Configuration
# Secret key for JWT token verification
# IMPORTANT: Must match the JWT_SECRET used in the backend
# Generate a strong, random string for production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Node Environment
# Options: development, production, test
# Next.js automatically sets this, but you can override if needed
# NODE_ENV=development

# ============================================
# Optional: Future Integrations
# ============================================

# Stripe Configuration (for payment integration)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Note: Cloudinary uploads are handled by backend
# Frontend only needs image domains configured in next.config.ts (already done)